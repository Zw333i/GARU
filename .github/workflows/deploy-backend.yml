name: Deploy Backend to Render

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy
        run: |
          curl -s "https://api.render.com/deploy/srv-d63f65ogjchc7391bt3g?key=-oqDAQEdBZA"
          echo "Deploy triggered successfully"
